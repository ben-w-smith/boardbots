# BoardBots Claude Memory

This file serves as the onboarding memory for Claude Code.

## Project Context

The primary source of truth for the project context, architecture, tech stack, and build commands is the universal `AGENTS.md` standard.

@import AGENTS.md

## Editing Context

When editing files inside `packages/engine/`, `packages/server/`, or `packages/client/`, you must read the `AGENTS.md` file located inside that directory for highly specific, domain-level friction points and architectural patterns.
